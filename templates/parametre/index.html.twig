{% extends 'base.html.twig' %}

{% block title %}Liste des paramètres{% endblock %}

{% block body %}
<div class="card shadow-sm">
    <div class="card-header bg-primary text-white d-flex justify-content-between align-items-center">
        <h1 class="h3 mb-0 text-white">Liste des paramètres</h1>
        <a href="{{ path('app_parametre_new') }}" class="btn btn-light btn-sm">
            <i class="bi bi-plus-lg"></i> Créer un paramètre
        </a>
    </div>
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-striped table-hover align-middle">
                <thead class="table-light">
                    <tr>
                        <th>Id</th>
                        <th>Libellé</th>
                        <th>Valeur</th>
                        <th class="text-end">Actions</th>
                    </tr>
                </thead>
                <tbody>
                {% for parametre in parametres %}
                    <tr>
                        <td>{{ parametre.id }}</td>
                        <td>{{ parametre.libelle }}</td>
                        <td>{{ parametre.valeur }}</td>
                        <td class="text-end">
                            <div class="btn-group" role="group">
                                <a href="{{ path('app_parametre_show', {'id': parametre.id}) }}" class="btn btn-info btn-sm text-white" title="Voir">
                                    <i class="bi bi-eye"></i>
                                </a>
                                <a href="{{ path('app_parametre_edit', {'id': parametre.id}) }}" class="btn btn-warning btn-sm text-white" title="Modifier">
                                    <i class="bi bi-pencil"></i>
                                </a>
                            </div>
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="2" class="text-center py-4">Aucun paramètre trouvé</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>
{% endblock %}
