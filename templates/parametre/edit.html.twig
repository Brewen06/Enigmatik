{% extends 'base.html.twig' %}

{% block title %}Edit Parametre{% endblock %}

{% block body %}
<section class="edit-page">
    <div class="edit-page__card">
        <div class="edit-page__header">
            <h1 class="edit-page__title">Modifier le paramètre</h1>
            <p class="edit-page__subtitle">Affinez ce paramètre puis sauvegardez la mise à jour.</p>
        </div>

        {{ include('parametre/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <div class="edit-page__actions mt-4 d-flex flex-wrap gap-2">
            <a href="{{ path('app_parametre_index') }}" class="btn btn-outline-secondary">Retour à la liste</a>
            {{ include('parametre/_delete_form.html.twig') }}
        </div>
    </div>
</section>
{% endblock %}
