{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

<<<<<<< HEAD
{% block stylesheets %}
    <script src="https://cdn.tailwindcss.com"></script>
{% endblock %}

{% block body %}
<div class="min-h-screen flex items-center justify-center bg-gray-100">
    <div class="bg-white/90 backdrop-blur-md p-10 rounded-2xl shadow-xl max-w-xl w-full text-center">
        <h1 class="text-4xl font-extrabold bg-clip-text text-gray-700 mb-6">
            Bienvenue sur Enigmatik !
        </h1>

        <a href="/equipe/creer"
            class="inline-block px-6 py-3 text-lg font-semibold text-red-500 rounded-lg hover:scale-105 transform transition duration-300 border-2 border-red-500 hover:bg-red-500 hover:text-white"
            title="Créer une équipe">
             Créez votre équipe.
        </a>
    </div>
</div>
{% endblock %}
=======
{% block body %}
    <div class="landing-shell tw-text-center tw-mt-10 tw-p-10 tw-bg-gray-100 tw-rounded-xl tw-shadow-lg">
        <h1 class="tw-text-5xl tw-font-bold tw-mb-6 tw-text-blue-600">Bienvenue sur Enigmatik</h1>
        <p class="tw-text-xl tw-text-gray-700 tw-mb-8">Découvrez des énigmes sur l'informatique et testez vos connaissances en équipe !</p>
        
        <div class="tw-flex tw-justify-center tw-space-x-4">
            {% if equipe is defined and equipe %}
                <div class="tw-bg-white tw-rounded-lg tw-shadow tw-p-4 tw-mr-4 tw-flex tw-items-center tw-gap-3">
                    {% if equipe.avatar %}
                        <img src="{{ asset(equipe.avatar.image) }}" alt="{{ equipe.avatar.nom }}" class="tw-w-12 tw-h-12 tw-rounded-full tw-object-cover">
                    {% endif %}
                    <div class="tw-text-left">
                        <p class="tw-text-xs tw-text-gray-500 tw-uppercase tw-mb-0">Équipe</p>
                        <p class="tw-font-semibold tw-text-gray-800 tw-mb-0">{{ equipe.nom }}</p>
                    </div>
                </div>
                <a href="{{ path('app_jeu_index', {'equipe_id': equipe.id}) }}" class="btn btn-success tw-inline-flex tw-items-center tw-justify-center tw-font-bold tw-leading-none tw-px-8 tw-py-3 tw-rounded-full tw-shadow-md hover:tw-shadow-xl tw-transition tw-duration-300 tw-whitespace-nowrap">Reprendre la partie</a>
            {% else %}
                <a href="{{ path('app_equipe_creer') }}" class="btn btn-primary btn-lg tw-font-bold tw-px-8 tw-py-3 tw-rounded-full tw-shadow-md hover:tw-shadow-xl tw-transition tw-duration-300">
                    Créer votre équipe
                </a>
            {% endif %}
        </div>
    </div>
{% endblock %}
>>>>>>> Connexion
