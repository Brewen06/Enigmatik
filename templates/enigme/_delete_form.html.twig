<form method="post" action="{{ path('app_enigme_delete', {'id': enigme.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ enigme.id) }}">
    <button class="btn">Delete</button>
</form>
